{
  "name": "todo-worker",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "esbuild src/worker.ts --bundle --outfile=dist/index.js --format=esm --platform=browser"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}